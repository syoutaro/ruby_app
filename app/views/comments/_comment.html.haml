.p-comment__item
  %p= simple_format(comment.comment)
  .p-comment__bottomLine
    %span= comment.user.username
    %span= comment.created_at.to_s(:datetime)
    %span= link_to '削除', comment, method: :delete, data: { confirm: '削除してよろしいですか？' }